within ModelicaCompliance.Scoping;

package Visibility
  extends Icons.TestPackage;

  annotation(Documentation(info = "<html>This package contains tests of the
    rules for public and protected elements described in section 4.1 of the
    specification.</html>"));
end Visibility;
