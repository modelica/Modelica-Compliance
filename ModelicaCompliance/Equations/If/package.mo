within ModelicaCompliance.Equations;

package If
  extends Icons.TestPackage;

  annotation(Documentation(info = "<html>This package tests the if-equation
    structure defined in section 8.3.4 of the specification.</html>"));
end If;
